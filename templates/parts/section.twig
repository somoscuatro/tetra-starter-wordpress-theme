<section class="{{ background ? 'bg-' ~ background : '' }} {{ foreground ? 'text-' ~ foreground : '' }} {{ section_classes }}" {{ block.anchor ? "id='" ~ block.anchor ~ "'" : '' }}>
	<div class="section-container container mx-auto py-10 md:py-20 {{ section_container_classes }}">
		{% block section_content %}
		{% endblock %}
	</div>
</section>
